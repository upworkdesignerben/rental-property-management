# Local development only. Copy to .env and choose your own strong password.
# Use 8+ characters with uppercase/lowercase letters, numbers and symbols.
# Avoid double quotes in the password (it is quoted inside the connection string).
MSSQL_SA_PASSWORD=LocalDev_Only_2026!
SQLSERVER_PORT=1433
WEB_PORT=8080
